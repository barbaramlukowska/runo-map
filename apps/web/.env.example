# Base URL of the Express API (server-side fetch in Next).
API_URL=http://localhost:3001

# Public: base URL the browser uses to POST to the API (Next inlines NEXT_PUBLIC_* at build).
NEXT_PUBLIC_API_URL=http://localhost:3001
