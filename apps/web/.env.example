# Public: base URL the browser uses to reach the API (Next inlines NEXT_PUBLIC_* at build).
# Not a secret — same as tile keys, this is public configuration.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Stadia Maps key, restricted to the production domain. Leave empty for localhost.
NEXT_PUBLIC_STADIA_API_KEY=
